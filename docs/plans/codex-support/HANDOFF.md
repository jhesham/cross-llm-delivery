# Current handoff

Updated 2026-09-24. **T01–T12, T13A, and M1–M3 are complete. T13 is split into T13A/T13B; parent T13 remains open.** Branch `refactor/codex-support`, remote `public`; T13A code/test head `adbaf0c`, closing docs follow it. Stop for the user's explicit token-availability confirmation before T13B. No main merge, release, real user-global install, or Codex live-model call.

T13A added `generator/install_codex.py` and a Codex section in `INSTALL.md`. The installer explicitly targets `<scope-root>/.agents/skills/<skill-name>` for either repository or user scope, previews exact paths, copies bundles, records ownership hashes, and refuses collisions, symlink escapes and local edits. Python bytecode caches generated by running the vendored driver are excluded from source ownership. [T13A evidence](T13A-EVIDENCE.md) and [split plan](T13-SLICES.md) have the implementation and validation details. The Kimi K3/OpenCode CLD attempt was collected and independently integrated; combined reported usage was **641,634 tokens / USD 0.7045932** with one production attempt and no fallback. Preserve ignored `.cld/t13a-dogfood/` evidence.

The committed acceptance file was red before dispatch. Local focused suite: 13 passed, one elevated-Windows permission skip. Local full offline suite passed before the final backup-cleanup fault-injection case; focused tests passed after it. A generated Codex/OpenCode bundle completed preview → install → nested-cwd driver invocation → update → uninstall in a disposable repo with spaces. Final [Windows/Ubuntu CI](https://github.com/jhesham/cross-llm-delivery/actions/runs/35891188570) passed full tests and Claude/Codex generator plus Codex plugin-package smoke on each OS.

T12 remains complete: standalone Codex skill generation under ignored `dist/codex/`, portable Codex plugin packages under ignored `dist/plugins/codex/`, verified host metadata, Claude default compatibility, and [green Windows/Ubuntu CI](https://github.com/jhesham/cross-llm-delivery/actions/runs/35849789785). [T12A evidence](T12A-EVIDENCE.md); [T12B evidence](T12B-EVIDENCE.md). Retain ignored `.cld/t12*-dogfood/` trees.

When the user confirms tokens, begin T13B by reading [the split plan](T13-SLICES.md), [T13 gate](04-CODEX-HOST.md#t13--codex-installation-and-discovery), and the tracker. T13B owns a generated repo plugin marketplace, concise repo `AGENTS.md`, and actual Codex CLI discovery from a nested cwd. Commit red acceptance and a bounded Kimi K3/OpenCode contract before dispatch. Keep installs disposable and preserve the existing Claude route. T17 follows T13.
