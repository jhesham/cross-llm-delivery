# Current handoff

Updated 2026-09-24. **T01–T13, T17A and M1–M3 are complete. Parent T17 remains open; T17B awaits the user's explicit token-availability confirmation.** Branch `refactor/codex-support`, remote `public`; T17A packaging source fixes `ae0556a`, `e979372`, followed by closing evidence/docs. No main merge, release or real user-global Codex install. T14 follows T17 because its cross-host acceptance depends on installable wheel and bundle artifacts.

T17A fixed R07: `pyproject.toml` now packages the six provider Markdown resources in wheel and sdist. The committed artifact test builds from a copied source tree, installs the wheel into a disposable target, then loads all three providers and runs CLI help from outside the checkout under `python -I -S`. Focused and local full offline suites passed. A separate sdist-to-wheel rebuild retained all six resources. Local verification artifact hashes are in [T17A evidence](T17A-EVIDENCE.md). Initial Windows Python 3.11 CI exposed an old setuptools rejecting the existing SPDX license string during no-isolation tests; the build/dev minimum is now 77. Final Windows/Ubuntu CI passed full tests and generator smoke on the corrected source commit.

Dogfood used exact `opencode:opencode/kimi-k3`, one validation probe and one production attempt, no fallback/retry: **224,951 reported tokens / USD 0.3175866** combined. CLD retained the candidate as `needs_repair` because `pyproject.toml` is a protected pytest configuration input, even though the task contract listed it as editable. The lead reviewed and manually applied the one-file package-data diff; CLD protection was not weakened and there was no second model call. Ignored ledger/attempt evidence remain under `.cld/t17a-dogfood/` and `.cld/runs/6a333c21229d46de88f52ae674d60c89/`.

Next, read [T17B in the split plan](T17-SLICES.md#t17b--bundle-matrix-and-tracked-freshness), the [T17 parent gate](06-PACKAGING-RELEASE.md#t17--wheel-bundles-and-ci), and the [tracker](TRACKER.md). T17B owns the six host/provider bundle matrix, Codex plugin manifests/catalog and existing tracked Claude-plugin regeneration drift; T17C later owns the wider Python/OS CI and compatibility gate. Do not begin T17B until the user confirms tokens. Preserve the existing Claude default route and exact Kimi model ID; do not silently substitute a provider. T14 remains blocked on parent T17 completion.
