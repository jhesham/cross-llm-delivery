# Current handoff

Updated 2026-09-24. **T01–T13 and M1–M3 are complete. T17 is next, but requires the user's explicit token-availability confirmation.** Branch `refactor/codex-support`, remote `public`; T13B code/test merge `f4b9551`, followed by closing evidence/docs. No main merge, release, real user-global Codex install, or Codex live-model call.

T13B added a deterministic local catalog at `<out-root>/.agents/plugins/marketplace.json` beside three portable Codex plugin packages, plus repository `AGENTS.md`, install instructions, and offline CI catalog checks. The default Codex output is ignored `dist/plugins/`; the tracked Claude plugin route is unchanged. Five focused acceptance cases passed. The installed Codex 0.155.1 app-server discovered the generated OpenCode skill and all three local plugins from a nested cwd in a disposable Git repo with spaces; there were no marketplace load errors. The vendored driver resolved there and emitted a no-dispatch JSON dry run. [T13B evidence](T13B-EVIDENCE.md) holds the paths and host result; [T13A evidence](T13A-EVIDENCE.md) covers the standalone install/update/uninstall route, which remains the IDE path.

The exact dogfood executor was `opencode:opencode/kimi-k3`: one validation probe and one accepted production attempt, no fallback/retry. CLD collected the four-file candidate and independently integrated it. Combined reported usage was **542,087 tokens / USD 0.5650458**, including validation; ignored ledger/artifacts remain under `.cld/t13b-dogfood/` and `.cld/runs/94228de0b0d04bc3841e7a43a5d7fd23/`. Local focused and full offline suites passed; the final Windows/Ubuntu CI run includes both host generator smokes, Codex plugin packaging and catalog check.

An extra no-argument Claude generator smoke reconfirmed that tracked `plugins/` currently drifts when regenerated from source. The probe's changes were restored; the tree was clean beforehand and remains clean. T17's planned committed-bundle freshness gate owns this pre-existing issue. It also owns wheel/sdist isolation, package data, broader Python CI and bundle matrix. Do not start T17 until the user confirms token availability. Read [T17](06-PACKAGING-RELEASE.md#t17--wheel-bundles-and-ci), the [tracker](TRACKER.md), and this handoff at restart. Preserve the existing Claude route and exact model ID; do not silently substitute a provider.
