# T13B evidence — Codex marketplace and host discovery

T13B closes the second T13 child: a generated local Codex marketplace catalog, concise repository `AGENTS.md`, and actual installed Codex discovery. The standalone route from T13A remains available for the IDE. No real user-global skill or plugin was installed, and the Codex host probe made no inference call.

The lead committed five red acceptance cases and the bounded dogfood contract before dispatch (`3259207`, `8409eda`; bounded assertion output `49e987d`). A clean detached baseline passed `CandidateVerifier.preflight` with fingerprint `ff7ae05ac1c7685e1277a0b8a0508ccbba76241bcc0da23487b67e1f053e5473`. The exact executor `opencode:opencode/kimi-k3` ran one validation probe and one production attempt, without fallback or retry. CLD collected only the four allowed source/doc/CI files as candidate `a13fb14f236845f844679cd102aa65ecf553c3d0`. Independent acceptance and integration passed at merge `f4b9551b305dd618b1857569dd5178a2ee5b6319`; the branch fast-forwarded to that merge. The ignored ledger and journal are under `.cld/t13b-dogfood/` and `.cld/runs/94228de0b0d04bc3841e7a43a5d7fd23/`.

Validation used **49,475 tokens / USD 0.051801**; production used **492,612 tokens / USD 0.5132448**. Combined reported usage was **542,087 tokens / USD 0.5650458**. Input, output and cache-read usage were known; the ledger reports no overrun.

`generator/build_plugins.py --host codex` now creates three portable local plugin packages at `<out-root>/codex/` and `cross-llm-delivery-codex` at `<out-root>/.agents/plugins/marketplace.json`. Its local paths resolve inside the same output root. Missing input bundles leave prior output untouched. The default Codex output remains ignored `dist/plugins/`; no-argument Claude output remains the tracked `plugins/` route. The five focused acceptance cases passed after integration. A real generated-bundle run and repeat generator run passed locally; the second reported `none (idempotent)`. The exact CI catalog assertion passed locally.

An extra lead smoke of the unchanged no-argument Claude generator exposed pre-existing tracked-bundle drift: regenerating `plugins/` from the current engine changed committed files. The lead restored only that generated tree, which was clean before the probe; it is not part of the T13B candidate. T17 explicitly owns committed-bundle freshness and drift enforcement, so this is carried into that task rather than folded into the Codex marketplace slice.

For host evidence, generated Codex bundles were copied into an ignored disposable Git repository whose path includes spaces. An installed `codex-cli 0.155.1` app-server received `skills/list` and `plugin/list` with a nested `cwds` path. It reported `cross-llm-opencode` as an enabled `scope: repo` skill under that repository's `.agents/skills/` directory. It separately loaded `cross-llm-delivery-codex` from `.agents/plugins/marketplace.json`, found exactly `cross-llm-antigravity`, `cross-llm-opencode`, and `cross-llm-cursor` at the expected local package paths, and returned `marketplaceLoadErrors: []`. The vendored `run_delivery.py --help` resolved from the nested cwd; its `--dry-run --json --host codex` returned a one-layer `T13B` schedule without dispatch. `codex plugin list` did not show the disposable marketplace because that CLI command lists configured marketplaces; `INSTALL.md` documents explicit `codex plugin marketplace add` for users who choose that route. The app-server `plugin/list` with `cwds` provided actual project-catalog discovery without changing user configuration.

The [official Build skills guide](https://learn.chatgpt.com/docs/build-skills), [plugin packaging guide](https://developers.openai.com/plugins/build/plugins), [plugin surface guide](https://learn.chatgpt.com/docs/plugins), [marketplace format example](https://github.com/openai/codex/blob/main/codex-rs/skills/src/assets/samples/plugin-creator/references/plugin-json-spec.md), and [app-server project marketplace tests](https://github.com/openai/codex/blob/main/codex-rs/app-server/tests/suite/v2/plugin_list.rs) were checked for T13. The CLI and app-server are supported local surfaces for this route; the IDE uses standalone skills and was not claimed to support plugins.

The local full offline suite completed at 100% with one elevated-Windows read-only permission skip and no failures. [CI on the T13B code/test merge `f4b9551`](https://github.com/jhesham/cross-llm-delivery/actions/runs/35933244678) passed on Windows and Ubuntu: full test step, Claude and Codex skill generation, Codex plugin packaging, and the new three-source catalog assertion on both OSes. The closing documentation commit follows that tested code/test merge.
