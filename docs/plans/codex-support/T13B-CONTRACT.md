# T13B implementation contract — Codex marketplace and maintainer entrypoint

Implement only the source portion of T13B. The lead will separately run the installed Codex host probe and record its evidence. Edit exactly `generator/build_plugins.py`, `INSTALL.md`, `.github/workflows/ci.yml`, and new repository-root `AGENTS.md`. Do not edit tests, this contract, tracked Claude plugins, engine, standalone installer, or user-home files. Do not invoke a Codex model, install a real plugin, commit, push, recurse into CLD, or dispatch another provider.

For `python generator/build_plugins.py --host codex`, generate a local marketplace catalog at `<out-root>/.agents/plugins/marketplace.json` **alongside** the existing portable plugin packages at `<out-root>/codex/cross-llm-<provider>/`. The no-argument output root remains ignored `dist/plugins`; explicit `--out-root` remains supported. Catalog name is `cross-llm-delivery-codex`, with stable `interface.displayName`. Include exactly the three supported providers with `source: {source: local, path: ./codex/cross-llm-<provider>}` relative to the marketplace root, and official `policy.installation: AVAILABLE`, `policy.authentication: ON_INSTALL`, `category: Productivity`. Generated JSON is deterministic and repeatable. Preflight all three input bundles before changing output, and do not touch an adjacent Claude plugin tree. No-argument Claude generation retains its exact existing behavior and does not create a Codex catalog.

Use the already generated portable root `plugin.json` and `skills/<name>/SKILL.md` packages. Do not make a second metadata source, add an MCP server, or claim IDE plugin support. Update CI's Codex plugin packaging smoke to check the catalog and its local source paths without any Codex installation or network calls. Add an `INSTALL.md` Codex plugin route with the build commands, a `codex plugin marketplace add` path, how to list/install on supported local surfaces, and an explicit note that the IDE uses the standalone skill route. State that this command changes the user's configured Codex marketplace and is never a generator side effect.

Add concise repository `AGENTS.md` (at most 80 lines) with maintainer entrypoints (`IMPLEMENTATION_PLAN.md`, `HANDOFF.md`), source paths and generated-file policy, focused tests, and the task checkpoint. It applies to this repository only; do not copy user-wide instructions or embed the entire initiative. Make the committed `tests/test_t13b_marketplace.py` pass. Run focused tests only and finish promptly.
