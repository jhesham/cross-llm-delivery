# T17B evidence — bundle matrix and tracked freshness

T17B closes the generated-bundle part of parent T17. T17C still owns the wider Python/OS matrix and compatibility gate; T14 remains dependent on parent T17.

The lead built all six `claude-code`/`codex` × `antigravity`/`cursor`/`opencode` bundles into a fresh ignored root. Each generator build ran its isolated vendored-provider smoke. The committed T17B acceptance also runs every generated `scripts/run_delivery.py --help` from outside the bundle, checks host metadata and paths, packages the three Codex bundles, and checks their manifests and contained local catalog sources. These are offline checks; no live provider or real user-global plugin installation was attempted.

Before regeneration, the committed Claude `plugins/` tree was missing 48 generated files and differed in 60 existing files; there were no obsolete files in that tree. The acceptance test for committed freshness failed on exactly that drift, while the matrix and drift-detection cases passed. The red contract and CI step were committed at `7997ecb`. The lead then ran the documented Claude generator and plugin packager; generated output was committed at `d09a2ab` (108 files, including 48 new vendor files). A second packaging pass reported `none (idempotent)`. The freshness checker packages into a temporary root, compares the full file set and contents against tracked plugins, normalizes Windows line endings during comparison, and relies on the existing package generator to normalize the embedded Git SHA banner. It never modifies tracked plugins. CI runs it after building Claude bundles. The default Claude route and ignored Codex output route are unchanged.

This slice was lead implemented. Delegating mechanical regeneration of the tracked generated tree to Kimi would have enlarged candidate scope without useful independent implementation; no provider call or model substitution was made. Token and provider cost for this slice are therefore unavailable/zero provider usage, respectively.

Verification: the focused T17B suite passed locally (`3 passed`), the full offline suite completed with exit code 0 (one expected read-only-permissions skip on this Windows runtime), `git diff --check` was clean, and a second tracked-plugin packaging pass was byte-idempotent. [CI at source SHA `d09a2ab`](https://github.com/jhesham/cross-llm-delivery/actions/runs/35959871810) passed on both Windows and Ubuntu: full offline suite, Claude and Codex bundle generators, the new tracked Claude freshness gate, Codex plugin packaging, and catalog smoke. The closing documentation commit follows the tested source SHA; it changes no generator, test, or bundled product file.
