# T13A evidence — standalone Codex skill installation

T13A is the first of two bounded T13 children. The full T13 host-discovery and plugin-marketplace gate remains T13B. The user authorized T13, then confirmed continuation while T13A validation was running. No subagent or alternate model was used.

The lead committed the contract, split plan and an acceptance file with nine red cases and one elevated-Windows permission skip before dispatch (`b3b1c52`; parseable dogfood plan `8c4e9e7`). An isolated detached worktree passed `CandidateVerifier.preflight`, proving the acceptance failed by assertions on the committed baseline and recording fingerprint `20d2b55a61c238949f2a6248efdc61eb5ee37f935fa86795d60490e845a1cd78`. The exact executor was `opencode:opencode/kimi-k3`, with one validation probe and one production attempt, no fallback or retry. Model availability was checked with OpenCode 1.18.29. Local project evidence/ledger are under ignored `.cld/t13a-dogfood/`; no real user-home installation was made.

Validation usage: **49,943 tokens, USD 0.0349434**. Production usage: **591,691 tokens, USD 0.6696498**. Combined ledger total: **641,634 tokens, USD 0.7045932**, with known input/output/cache-read categories and no usage overrun. Run ID `7fc57aef772240e4b5628d74d7b72cd6`.

CLD collected only the allowed installer and `INSTALL.md` files as candidate `2e04dd6c9078283f3776f7a6aa5fb627ea9a670d`. Independent integration re-ran the acceptance from the committed base and published merge `da2c7819d6cd860d43c3140a6c0534d2bba978fc` (journal `.cld/runs/7fc57aef772240e4b5628d74d7b72cd6/integration/1dd71fb60c924a2bb4852ef9219d91fa`). The branch received that accepted change as `f3ab171`.

Lead review corrected the outdated Claude banner note and checked containment of installer staging/backup paths. A real disposable generated bundle then exposed a missing case: invoking its Python driver made `__pycache__` files, so the original ownership check blocked uninstall. A new regression failed first. The fix excludes bytecode caches from installed source ownership while still rejecting non-cache additions and local edits (`542f962`). Target paths became absolute for relative scope input (`0f64225`). A fault-injection regression then showed that a locked old backup after update publication was reported as a failed install. The installer now returns `updated` with `cleanup_warning` and retains the backup for inspection (`adbaf0c`). Source and target symlinks, same-name collisions, edits, path spaces and nested working directories have focused coverage. The read-only permission test skips on this elevated Windows runtime because chmod does not deny writes; Ubuntu CI exercises it as a non-elevated user.

The [official Build skills guide](https://learn.chatgpt.com/docs/build-skills) was refreshed on 2026-09-23. It documents repository ancestor `.agents/skills` discovery through Git root, user `$HOME/.agents/skills`, explicit `/skills` or `$skill` invocation, and standalone support in Codex CLI and IDE. The [plugin guide](https://learn.chatgpt.com/docs/plugins) confirms the IDE extension does not support plugins. `INSTALL.md` now uses these paths and documents a preview-first copy, explicit scope root, update and uninstall. The source bundle and plugin paths remain separate.

Local focused suite: 13 passed, one elevated-Windows permission skip. Local full offline suite passed before the final backup-cleanup fault-injection addition; the focused suite passed after it. A generated Codex/OpenCode bundle in an ignored disposable repo with spaces completed preview → install → `run_delivery.py --help` from a nested cwd → update → uninstall; the target was absent afterward. No live model call was made by that smoke test. [Final CI on `adbaf0c`](https://github.com/jhesham/cross-llm-delivery/actions/runs/35891188570) passed on Windows and Ubuntu: full test step, Claude generator smoke, Codex generator smoke and Codex plugin-package smoke on both OSes. The earlier CI runs on superseded review-fix commits were canceled; only the final green run is the closing gate. Actual Codex host discovery and repo plugin marketplace remain T13B; parent T13 is open.
