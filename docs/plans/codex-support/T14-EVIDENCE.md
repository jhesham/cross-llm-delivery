# T14 cross-host acceptance — closing evidence

T14's offline flow, CLI and IDE host checks, and one pinned live canary passed. The Windows host rehearsal is separate from the Windows/Ubuntu offline CI matrix. The final pushed SHA and CI result are recorded below.

`tests/integration/test_t14_cross_host.py` creates a disposable Git repository with spaces in its path and commits a two-layer plan plus acceptance/integration tests. Separate Python CLI processes drive preview, A step, status after stop, repeated step, A integration, B step, B integration, and final status from another working directory. The fake executor writes real files and uses the production diff capture. A repeated step before A integration returns a failed/deferred gate but does not dispatch B; final ledger attempt counts are exactly A=1 and B=1, and the gate is `passed` with `next_action=complete`. The configured worktree root is inside the disposable repo. Additional probes reject a worktree root containing the repo and an unusable file path as structured `blocked` responses with zero dispatch.

All three existing provider contracts were instantiated from isolated generated Claude and Codex bundles (six combinations); each vendored process loaded only its own provider. These are offline contract checks, not live calls. The installed Codex 0.155.1 app-server again discovered `cross-llm-opencode` at repo scope from a nested directory in a disposable repository with spaces, with no marketplace load errors. That installed skill's vendored driver resolved from the nested directory and returned a two-layer JSON preview.

The actual VS Code 1.139.0 Codex extension (`openai.chatgpt` 26.917.62051) then opened a disposable Git workspace under ignored `.cld/t14-ide-check/ide repo with spaces/`. A fresh generated Codex bundle was installed at that workspace's `.agents/skills/cross-llm-opencode`; no user-global skill was installed. In the Codex IDE panel, typing `$cross-llm-opencode` offered **Cross-LLM Delivery (opencode executor)** with its generated description, and selecting it inserted a skill chip. [Skill-picker capture](evidence/T14-codex-ide-skill-picker.png). The bounded prompt asked the IDE to run only the vendored `scripts/run_delivery.py` against `plan.md` with `--repo . --dry-run --json --host codex`, and prohibited executor dispatch, edits, and provider calls. The IDE invoked the repo-local script and reported its resolved absolute path, `gate: pending` (exit code 0), and `layers: [["A"], ["B"]]`. [Result capture](evidence/T14-codex-ide-result.png). This is an observed IDE skill invocation, separate from the prior app-server discovery and shell dry-run. The Codex IDE lead model label was `GPT-6 Astra Medium`; its token and cost counters were not exposed. No new Kimi/provider dispatch occurred in this IDE check. A separate Claude memory extension created an untracked `AGENTS.md` inside the disposable workspace during the UI session; the delivery driver did not create a ledger or modify the tracked product checkout.

Claude Code 2.1.281 was launched interactively with `--plugin-dir` and no inference prompt. Its `/skills` view showed the existing `cross-llm-opencode` name as a plugin skill; the installed user skill names also remained visible. The tracked plugin's vendored driver returned the same two-layer JSON preview with `--host claude-code`. Claude's `plugin validate` initially reported missing skill frontmatter because the provenance comment preceded `---`. The source template and all three tracked Claude bundles now put YAML first, with provenance immediately below it; the validator no longer reports that warning. It still suggests an optional plugin manifest version, which is unrelated to skill discovery.

The user-selected live model remained exactly `opencode:opencode/kimi-k3`; `opencode models opencode` listed it. A one-slice canary was prepared in ignored `.cld/t14-canary/` with committed acceptance tests, an exact model pin, a two-attempt/600,000-token admission budget (300,000 tokens reserved per attempt), and `--validation-policy allow`. The first preflight stopped with `Invalid validation status` because an older user-global evidence record was `untested`. A focused compatibility fix maps that inconclusive legacy status to `revalidate`, which grants no validation proof. Its regression test covers the old record alongside existing `known-bad` and `proven` aliases. The normal CLI then revalidated Kimi K3 and dispatched one production attempt. The user-global evidence write retained existing records, normalized the old status to `revalidate`, and stored Kimi K3 as `verified`.

The production candidate changed only `value.py` to `VALUE = 42` and passed its committed acceptance test. CLD accepted commit `4eec34e7b7768121acc14cfe474be5afc77b815c`, then stopped at `integration_required`. A fresh CLI process reported that gate; another performed independent integration against `test_integration.py`, yielding commit `37a962d2cd30a5fa2a4771be74327d64df4572ac`. Final status was `passed`/`complete`, with no duplicate dispatch. OpenCode 1.18.29 ran on Windows; lead host provenance was `codex`. Usage reported by the provider: validation 1 attempt, 50,921 tokens, USD 0.0549198; production 1 attempt, 49,397 tokens, USD 0.050847; total 2 attempts, 100,318 tokens, USD 0.1057668. No usage field was unknown; costs are provider-reported, not an external billing reconciliation. The stopped state was after accepted work and before integration, not a mid-process kill.

No user-global skill/plugin installation or main-branch merge was made. The normal validation run updated the shared user-global evidence cache as described above. Lead-agent token use is unavailable.

Focused evidence/admission/T14 integration tests passed. The full local Windows Python 3.13 offline suite exited 0 after the final source changes, with one expected read-only-directory skip on this elevated runtime and two existing behavioral-judge deprecation warnings. Fresh Claude plugin output matched all three tracked packages; all three Codex skill bundles and the local plugin catalog rebuilt successfully. Four-job Windows/Ubuntu CI with both-host generator smoke passed at source/evidence commit `55632ce` ([run 36024793265](https://github.com/jhesham/cross-llm-delivery/actions/runs/36024793265)). The IDE closure changes only documentation and small evidence screenshots; a duplicate local suite run was interrupted after 17% to avoid repeating the already-green source test run. Final-SHA CI will cover the closure commit.

The IDE evidence and T14/M4 status were committed as `a266da4`. [Branch CI history](https://github.com/jhesham/cross-llm-delivery/actions/workflows/ci.yml?query=branch%3Arefactor%2Fcodex-support) provides the run at the final pushed documentation SHA; its four Windows/Ubuntu Python 3.11/3.14 jobs and generator/package gates must all pass before the handoff is considered settled.
