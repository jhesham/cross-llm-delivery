version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "monthly"
    # our >= constraints are intentional MINIMUMS - only propose bumps a resolver actually needs
    versioning-strategy: increase-if-necessary
